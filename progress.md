# Progress: Elementor MCP Server

## Overview
- **PRD:** prd.md
- **Started:** 2026-08-20
- **Status:** 🚧 **IN PROGRESS** — EMCP-001 underway

---

## Task Progress

| Task ID | Task Name | Status | Started | Completed | Notes |
|---------|-----------|--------|---------|-----------|-------|
| EMCP-001 | Docker Compose stack | ✅ COMPLETED | 2026-08-20 | 2026-08-20 | All 8 containers healthy; isolation test passes (renderer blocked from db/db-wp, reaches both WP apps); down/up cycle verified from cold |
| EMCP-002 | Sandbox provisioning and reset | ✅ COMPLETED | 2026-08-20 | 2026-08-20 | Both sandboxes provisioned; reset guard 7/7; reset verified idempotent; Elementor Pro pending zip (recorded, not blocking) |
| EMCP-003 | Plugin skeleton | ✅ COMPLETED | 2026-08-21 | 2026-08-21 | Activates clean on both sandboxes, WP_DEBUG on, zero notices; verified live |
| EMCP-004 | `GET /site` endpoint | ✅ COMPLETED | 2026-08-21 | 2026-08-21 | Verified live on both sandboxes incl. 401/403/200 paths; generation_default correctly differs v4 vs v3 |
| EMCP-005 | Node server skeleton | ✅ COMPLETED | 2026-08-21 | 2026-08-21 | verify:unit green (type-check+lint+test); live smoke test of built server also passed |
| EMCP-006 | MCP protocol conformance | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | verify:unit green (18 tests); live smoke test of all header/origin/method paths on the built server |
| EMCP-007 | First tool — `get_site_info` | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | Full hybrid path verified live on both sandboxes; generation_default correctly returns v4 vs v3 |
| EMCP-008 | Capture golden fixtures ⚠️ | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | Human-closed by explicit user decision. 8/10 captured; `nested-widget` and `pro-globals-dynamic` accepted as permanent blockers (real editor gap; missing Pro zip) — documented, not resolved |
| EMCP-009 | Fixture immutability guard | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | 3/3 tests green; guard verified to actually fire against a deliberately corrupted manifest entry, then restored |
| EMCP-010 | Freeze plugin REST contract v1 | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | verify:unit green (33 tests); live smoke test confirms normal operation unaffected |
| EMCP-011 | Verification harness split | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | verify:unit orchestrates all 3 projects (41 tests green); verify:live proven against real up/down/starting sandbox states |
| EMCP-012 | Local development documentation | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | User reviewed README and confirmed. Every command tested against the real stack, incl. a genuine fresh `mcp` container build |
| EMCP-013 | Database schema and migrations | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | Full up→down→up cycle proven against the real Postgres container, not just generated |
| EMCP-014 | Site registry | ✅ COMPLETED | 2026-08-24 | 2026-08-24 | Live round-trip proven (create/getBySlug/list) against real Postgres; mutation boundary enforced structurally, not just by convention |
| EMCP-015 | Credential encryption | NOT STARTED | | | |
| EMCP-016 | Grant resolution | NOT STARTED | | | |
| EMCP-017 | `GET /registry/snapshot` | NOT STARTED | | | |
| EMCP-018 | Committed snapshots and drift check | NOT STARTED | | | |
| EMCP-019 | Generation detection | NOT STARTED | | | |
| EMCP-020 | Normalized digest shape | NOT STARTED | | | |
| EMCP-021 | Label resolution and sanitisation | NOT STARTED | | | |
| EMCP-022 | Depth limiting | NOT STARTED | | | |
| EMCP-023 | `list_pages` | NOT STARTED | | | |
| EMCP-024 | `get_page_structure` | NOT STARTED | | | Token budget is a measured criterion |
| EMCP-025 | `get_element` | NOT STARTED | | | |
| EMCP-026 | `find_elements` | NOT STARTED | | | |
| EMCP-027 | `list_widgets` | NOT STARTED | | | |
| EMCP-028 | `describe_widget` | NOT STARTED | | | |
| EMCP-029 | `get_global_styles` | NOT STARTED | | | |
| EMCP-030 | V4 authoring spike ⚠️ | NOT STARTED | | | Timeboxed. Human-verified |
| EMCP-031 | Renderer service | NOT STARTED | | | |
| EMCP-032 | Egress filtering | NOT STARTED | | | |
| EMCP-033 | `POST /preview-token` | NOT STARTED | | | |
| EMCP-034 | `render_preview` | NOT STARTED | | | |
| EMCP-035 | Cache invalidation before capture | NOT STARTED | | | |
| EMCP-036 | Structural validation | NOT STARTED | | | |
| EMCP-037 | Snapshot capture and restore | NOT STARTED | | | |
| EMCP-038 | Ledger | NOT STARTED | | | |
| EMCP-039 | `list_changes` and bounded `rollback` | NOT STARTED | | | |
| EMCP-040 | Prove safety ring on minimal write ⚠️ | NOT STARTED | | | **Hard gate** — no mutating tool before this |
| EMCP-041 | Document hash compare-and-swap | NOT STARTED | | | |
| EMCP-042 | Post lock refusal | NOT STARTED | | | |
| EMCP-043 | `edit_elements` | NOT STARTED | | | |
| EMCP-044 | Idempotency keys | NOT STARTED | | | |
| EMCP-045 | Autosave and revision mechanics | NOT STARTED | | | |
| EMCP-046 | `create_page` and `update_page` | NOT STARTED | | | |
| EMCP-047 | `publish_draft` | NOT STARTED | | | Blocked on D3 |
| EMCP-048 | DSL grammar and schema | NOT STARTED | | | |
| EMCP-049 | Compiler core | NOT STARTED | | | |
| EMCP-050 | v3 emission | NOT STARTED | | | |
| EMCP-051 | v4 emission | NOT STARTED | | | |
| EMCP-052 | Responsive | NOT STARTED | | | Must not slip to a later section |
| EMCP-053 | `raw` supervision | NOT STARTED | | | |
| EMCP-054 | Decompiler and round-trip | NOT STARTED | | | |
| EMCP-055 | `validate_page_spec` / `apply_page_spec` | NOT STARTED | | | |
| EMCP-056 | Protected Resource Metadata | NOT STARTED | | | Blocked on D2 |
| EMCP-057 | 401 challenge and audience validation | NOT STARTED | | | Blocked on D2 |
| EMCP-058 | Scopes | NOT STARTED | | | Blocked on D2 |
| EMCP-059 | Per-site URLs, Claude.ai end to end | NOT STARTED | | | Blocked on D2, D4. Human-verified |
| EMCP-060 | `list_templates` / `save_as_template` | NOT STARTED | | | |
| EMCP-061 | `apply_template` | NOT STARTED | | | |
| EMCP-062 | Cross-sandbox portability | NOT STARTED | | | |
| EMCP-063 | `upload_media` / `list_media` | NOT STARTED | | | |
| EMCP-064 | `upload_reference_design` | NOT STARTED | | | Blocked on D1 |
| EMCP-065 | `extract_design_tokens` | NOT STARTED | | | |
| EMCP-066 | `compare_to_reference` | NOT STARTED | | | Blocked on D1 |

---

## Summary
- **Total Tasks:** 66
- **Completed:** 14
- **In Progress:** 0
- **Not Started:** 52

---

## Blocked Decisions

Not loop tasks — these need a human.

| # | Decision | Gates | Status |
|---|---|---|---|
| D1 | Reference-design ingestion — URL vs out-of-band upload | EMCP-064, EMCP-066, all visual criteria | OPEN |
| D2 | IdP selection against `solution.md` §9.3 | EMCP-056..059 | OPEN — long lead time, start now |
| D3 | Approval channel for `publish_draft` | EMCP-047 | OPEN |
| D4 | Production hosting — KMS, segmentation, TLS | EMCP-059, deployment | OPEN — phases 0–6 run on local Compose |

---

## Log

### 2026-08-20
- Design phase complete. `solution.md`, `Blueprints.md`, `ralphloop.md`, `prd.md`, `CLAUDE.md` written.
- `solution.md` revised after four independent technical reviews (Elementor domain, MCP protocol, security, delivery risk). Material corrections:
  - **V4 is GA and default for new installs since April 2026** — the create-path decision was inverted from "V3 containers only" to "the site's own generation".
  - **V4 detection must key on `widgetType`, not `elType`** — atomic content widgets are still `elType: widget`. Detecting on `elType` would silently corrupt nodes.
  - **V4 typed props nest** — flat `{$$type, value}` assumptions drop content.
  - **Draft-first on published pages means autosave revisions**, a second write path entirely.
  - **Capability-gated `tools/list` is not implementable** under MCP 2026-07-28; resolved by per-site connector URLs, which also binds sessions to one site and fixes the OAuth audience model.
  - **Prompt injection was unaddressed** and is the system's largest risk; §9.1 added.
  - **Nativeness demoted from a build gate to a warning** — as a gate it rewards one large HTML blob over several small ones.
- PRD written: 66 tasks across ten sections. Detail decreases with distance by design.
- GitHub issues/project setup attempted, then set aside at the user's direction ("just start developing our project") to prioritize implementation. State left mid-cleanup: repo `abdullahjay/WP_lementor_MCP` has a project "Elementor MCP" (#2, renamed from a pre-existing "ELementor MCP", now carries a Module single-select field) plus a duplicate project #3 not yet deleted, and 3 test issues (EMCP-001..003) whose body text has a known em-dash encoding bug. None of this blocks implementation; revisit when convenient.
- **EMCP-001 — Docker Compose stack — DONE.** Wrote `docker-compose.yml`, `.env.example`, `scripts/db-wp-init.sh`, `scripts/test-renderer-isolation.sh`. Three-network design: `wp_net` (WordPress + db-wp + mcp), `data_net` (mcp + postgres + minio), `render_net` (renderer + both WordPress apps + minio + mcp) — renderer is on `render_net` only, per `solution.md` §9.5's isolation requirement. `mcp`/`renderer` run placeholder Node HTTP servers inline until EMCP-005/EMCP-031 land — deliberate, EMCP-001's job is topology, not app code.
  - Also added a `dev` service (`docker/dev/Dockerfile`: Node 20 + PHP 8.2 + Composer + git, repo mounted at `/workspace`, same network membership as `mcp`) as the workspace all future implementation tasks run from — not itself a PRD item, but infrastructure every later task depends on.
  - **Two real bugs found and fixed by actually running the stack, not just validating config:**
    1. Docker Desktop's daemon wasn't running on the host at all (`docker info`: CLI present, engine unreachable). Resolved as part of relocating Docker's WSL storage from C: to D: at the user's request (see below) — Docker Desktop was reinstated and confirmed healthy afterward, with all pre-existing unrelated containers/images/volumes verified intact post-move.
    2. `scripts/db-wp-init.sh` used `mysql -u root ...`, but the `mariadb:11` image no longer ships a `mysql` binary — it's `mariadb`. This failed **silently** (init script errored, MariaDB started anyway with no wp_v4_pro/wp_v3_free databases or users created), which meant both WordPress containers came up but returned 500 "Database Error" on every request, with healthchecks correctly catching it as `unhealthy`. Fixed the script, reset the stale `db-wp` volume (which had already been marked "initialized" by the failed first pass, so simply restarting wouldn't have re-run the script), confirmed both databases/users exist.
  - **Unrelated but user-directed: moved the WSL "Ubuntu" distro (which is where Docker Desktop's engine actually lives on this host — it's integrated with the default WSL distro rather than running its own dedicated data disk) from C: to D:.** Export → unregister → import → restored default user (`abdullah`) via `/etc/wsl.conf` → restored default distro → relaunched Docker Desktop → verified all pre-existing containers/images/volumes survived. C: free space 64GB → 69GB. This was explicitly requested mid-task and is orthogonal to the Elementor MCP project itself, but is recorded here since it's exactly the kind of environment change a later iteration needs to know happened.
  - **Final verification, all against the live stack:** all 8 containers (7 services + `dev`) report healthy; `scripts/test-renderer-isolation.sh` passes all 4 assertions (renderer blocked from `db:5432` and `db-wp:3306`, reaches `wp-v4-pro:80` and `wp-v3-free:80`); a full `docker compose down && docker compose up -d` cycle brings everything back healthy in ~30s with volumes intact, and isolation re-verified after.
- **EMCP-002 — Sandbox provisioning and reset — DONE.** Wrote `scripts/provision.sh`, `scripts/reset.sh`, `scripts/test-reset-guard.sh`. Added `wpcli-v4-pro`/`wpcli-v3-free` services to `docker-compose.yml` (`wordpress:cli` image, `profiles: [tools]` so they don't start with plain `up`, sharing each site's volume and DB credentials) since the official WordPress image ships no WP-CLI.
  - **Deliberate scope boundary:** provisioning does not hand-write `_elementor_data`. That format is exactly what `Blueprints.md` flags as unverified until EMCP-008 captures real fixtures — guessing at it in a provisioning script would undercut the reason that task exists. Provisioning creates plain WordPress pages as a baseline; Elementor content on top is EMCP-008's job.
  - **Real bug found and fixed:** `wordpress:cli` (Alpine, `www-data` uid 82) and `wordpress:*-apache` (Debian, `www-data` uid 33) disagree on the shared-volume owner's UID. Plugin installs failed with "could not create directory" / "plugin could not be found" — looked like a download failure, was actually a permissions mismatch. Fixed by pinning both wp-cli services to `user: "33:33"`.
  - **Real fact recorded, not assumed:** freshly installed Elementor from wordpress.org is **4.2.3**, not the 4.3.0 that `Blueprints.md`/`CLAUDE.md` were verified against. Also: `elementor_experiment*` options don't exist in `wp_options` at all on a fresh install — no rows returned — meaning generation defaults are code-level, not stored, until something (the admin UI, or a page save) touches them. Both are inputs EMCP-008 needs, not yet contradictions to resolve.
  - **Elementor Pro is not installed on `wp-v4-pro`** — `sandboxes/wp-v4-pro/elementor-pro.zip` was never supplied. `provision.sh` detects this, warns clearly, and continues with Elementor Free rather than failing. wp-v4-pro currently runs Free, same as wp-v3-free; the V4-vs-V3/Pro-vs-Free distinction the sandboxes exist to cover is not yet real until the zip is provided.
  - **Verification, all against the live stack:** both sandboxes provisioned (WP core + Elementor Free + baseline page); `test-reset-guard.sh` passes all 7 refusal cases (including a shell-injection-shaped argument, confirmed harmless since `reset.sh` uses positional-parameter case matching, not `eval`) — first run caught a bug in *the test itself* (comparing `docker compose ps` uptime strings, which drift naturally between snapshots) rather than a real regression, fixed by comparing container IDs instead; a real `reset.sh wp-v3-free` cycle verified end-to-end, then run a second time back-to-back to confirm idempotency (exit 0 both times, identical outcome).
- **No other implementation code exists yet. Nothing committed** — the user commits, nobody else.

### 2026-08-21
- **Real bug found and fixed before EMCP-003 could even be verified: the `db-wp` volume was stale/uninitialized** — same class of bug as EMCP-001's original MariaDB init failure, recurring on what was presumably a fresh volume from a prior session. `wp_v4_pro`/`wp_v3_free` databases and users didn't exist; both WordPress sites were 500ing on every request (health checks correctly showed `unhealthy`). Root cause confirmed by querying `db-wp` directly (`SHOW DATABASES` — only system schemas present). Fixed **without deleting the volume** — the sandbox's own permission classifier blocks `docker volume rm` outright regardless of user confirmation in chat, so instead ran `scripts/db-wp-init.sh`'s SQL manually against the live `db-wp` container to create the missing databases/users, then ran `scripts/provision.sh` (both sites) to actually install WordPress core and activate Elementor 4.2.3 into the now-empty-but-valid databases. Also found and started the `dev` container, which had never actually been started in this session (`docker compose ps` showed `Created`, not `Up` — `docker compose start dev` fixed it; no config bug, just never brought up).
- **EMCP-003 — Plugin skeleton — DONE.** Wrote `plugin/emcp.php`, `plugin/src/Plugin.php`, `plugin/uninstall.php`, `plugin/composer.json`.
  - `emcp.php`: plugin header with `Requires Plugins: elementor`, PHP 8.1+ guard, autoload-missing guard (points at `composer install`), boots `EMCP\Plugin::boot()` on `plugins_loaded` priority 20 — Elementor registers widgets on `plugins_loaded` at default priority, so booting later is required (`CLAUDE.md`).
  - `Plugin.php`: `boot()` checks `did_action('elementor/loaded')`; renders an admin notice and no-ops if Elementor is absent or inactive. No REST routes yet — that's EMCP-004.
  - `uninstall.php`: guarded no-op; EMCP-003 introduces no persistent state to clean up. Left a comment flagging that later tasks adding options/postmeta/tables must extend this file in the same change.
  - `composer.json`: PSR-4 `EMCP\` → `src/`, PHPUnit as a dev dependency for later unit tests.
  - **Infrastructure gap found and fixed as part of this task:** `docker-compose.yml` had no way to get `plugin/` into either WordPress container — only `sandbox-assets` was bind-mounted. Added `./plugin:/var/www/html/wp-content/plugins/emcp` to `wp-v4-pro`, `wp-v3-free`, `wpcli-v4-pro`, and `wpcli-v3-free`, matching the existing `sandbox-assets` mount pattern. Also added `WORDPRESS_DEBUG: 1` plus `WP_DEBUG_LOG`/`WP_DEBUG_DISPLAY` (log to file, don't render on page) to both WP services, since nothing previously turned `WP_DEBUG` on and the task's acceptance criterion requires it.
  - **Verification, all against the live stack, both sandboxes:** `wp plugin activate emcp` succeeds cleanly on both; `wp-content/debug.log` absent (zero notices) on both, both before and after activation; `wp eval 'did_action("elementor/loaded")'` confirms `true` on both, proving the boot-order dependency actually holds and not just in theory; separately verified the missing-Elementor path by deactivating Elementor on `wp-v3-free`, confirming `did_action('elementor/loaded')` flips to `false` (the condition `Plugin::boot()` guards on), then reactivating Elementor and reconfirming zero notices.
  - This is the first code in the repo written through Claude Code directly against the live sandboxes rather than design-phase documents.
- **`prd.md` Task 3 checked off; `progress.md` updated in the same pass** per `ONBOARDING.md`'s workflow.
- **EMCP-004 — `GET /site` endpoint — DONE.** Wrote `plugin/src/Rest/RestController.php`, `SiteController.php`, `Capabilities.php`; wired `RestController::register_routes()` onto `rest_api_init` from `Plugin::boot()`.
  - Every field in the response is read from Elementor's live runtime, not assumed — see `Blueprints.md` §6's new `GET /site` subsection for the confirmed shape and the source references (`Experiments_Manager::OPTION_PREFIX`/`STATE_*`, `Breakpoints_Manager::get_breakpoints()`, `elementor_element_cache_ttl` option, `elementor_css_print_method` option).
  - `pro_tier` returns `'free'` or `'pro-tier-unresolved'` rather than a boolean — Essential-vs-Advanced detection is explicitly deferred (`Blueprints.md` §12, `CLAUDE.md`'s "'Pro' is not a boolean") since neither sandbox has Elementor Pro installed to introspect a real tier signal against; guessing a class/method name here would violate the introspect-never-hardcode rule for no real benefit.
  - `Capabilities::can_read_site()` rejects any request without an `Authorization` header as cookie-authenticated (401 `emcp_cookie_auth_rejected`), independent of whether WordPress's own nonce check would have passed it — this is what makes the rejection "outright" per solution.md §9.7 rather than just nonce-checked. A present header still gates on `current_user_can('edit_posts')` (403 `emcp_forbidden` otherwise).
  - **Found and fixed a real gap in EMCP-002's own acceptance criteria while implementing this:** `wp-v4-pro` and `wp-v3-free` were provisioned identically — both plain Elementor Free with default experiment state — so nothing actually made them "cover both real forks" as EMCP-001/002 intended, and EMCP-004's "materially different values from each sandbox" criterion was unverifiable. Fixed in `scripts/provision.sh`: `wp-v3-free` now explicitly forces `elementor_experiment-e_atomic_elements`, `-e_opt_in_v4`, `-e_opt_in_v4_page` to `inactive` (real option keys, confirmed against Elementor's source — see `CLAUDE.md`), forcing it to the V3 fork. `wp-v4-pro` is left on Elementor's own defaults, which are already V4 (verified live: `e_atomic_elements`/`e_opt_in_v4*` are `default`→active on a fresh 4.2.3 install — confirms solution.md §5.1's "V4 is GA and default" claim against a real install, not just documentation).
  - **Found and fixed two more environment gaps, both now in `CLAUDE.md`:** (1) fresh WordPress defaults to Plain permalinks, so `/wp-json/...` 301-redirects instead of resolving — tested via the `/?rest_route=/…` fallback; a pretty permalink structure is still an open item for later, not blocking. (2) Application Passwords were silently unavailable (`wp_is_application_passwords_available()` false) because neither site is HTTPS and `WP_ENVIRONMENT_TYPE` wasn't `local`; added `WORDPRESS_CONFIG_EXTRA` (`WP_ENVIRONMENT_TYPE=local`, `WP_DEBUG_LOG`/`WP_DEBUG_DISPLAY`) to `docker-compose.yml` for future fresh provisions, but since both sandboxes' `wp-config.php` already existed in their volumes, the running sites needed a one-time `wp config set --type=constant` patch instead — `WORDPRESS_CONFIG_EXTRA` is a create-time-only mechanism, not applied on container recreate.
  - **Verification, all against the live stack, both sandboxes:** created real Application Passwords via `wp user application-password create`; confirmed `401` with no `Authorization` header, `403` for an authenticated subscriber (created and removed for the test), `200` with correct full payload for an authenticated admin; confirmed `generation_default` returns `"v4"` on `wp-v4-pro` and `"v3"` on `wp-v3-free` — the two sandboxes now genuinely diverge; `debug.log` stayed empty on both throughout.
- **`prd.md` Task 4 checked off; `progress.md` updated in the same pass.**
- **EMCP-005 — Node server skeleton — DONE.** Greenfield `server/`: `package.json`, `tsconfig.json`, `eslint.config.mjs`, `src/index.ts`, `src/http/{server,auth,correlation,logger}.ts`, `src/http/auth.test.ts`.
  - TypeScript strict (plus `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`), ESM (`NodeNext`), Fastify 5, `@modelcontextprotocol/sdk` installed as a dependency (not yet used — protocol wiring is EMCP-006).
  - **Version pin note:** `typescript` pinned to `^6.0.3`, not the newer `7.0.2` that `npm view` resolved to — `typescript-eslint@8.67.0`'s peer range is `>=4.8.4 <6.1.0` and doesn't support TS 7 yet. Revisit the pin once typescript-eslint catches up; not worth carrying a broken lint setup for a few months' head start on a major TS version.
  - `registerHeaderAuth()`: local dev auth, matches `.env.example`'s `MCP_HEADER_AUTH_TOKEN` — expects `Authorization: Bearer <token>`, rejects with `401` otherwise, `/healthz` exempted for container liveness probes. Refuses to start at all (`AuthConfigError`) if no token is configured, rather than silently running open. Comparison is via SHA-256 digest + `crypto.timingSafeEqual`, which also avoids leaking input length through timing (a direct string-length check would).
  - `correlationIdGenerator()`: `crypto.randomUUID()` as Fastify's `genReqId`, echoed back as `x-correlation-id` on every response and present on every structured log line via Fastify's request-scoped logger — this is the correlation ID the acceptance criterion asks for, verified in both the unit tests and a live smoke test.
  - `no-explicit-any` and `ban-ts-comment` (require a description) are enforced by `eslint.config.mjs`'s rules, not just convention — `npm run lint` fails the build if either is violated.
  - **Deliberately out of scope, matching the task's file list** (`server/src/index.ts`, `server/src/http/`, `package.json`, `tsconfig.json` — no `docker-compose.yml`): the `mcp` service is still EMCP-001's placeholder inline Node server. Wiring `server/` into a real container is natural EMCP-006/EMCP-007 work once there's an actual protocol handler and tool to serve, not before.
  - **Verification:** `npm run verify:unit` (type-check + lint + test) green — no network, matches this task's `Verify: unit`. Also went further than required and live-smoke-tested the actual built server inside the `dev` container (`npm run build && node dist/index.js`): `/healthz` 200 unauthenticated, unknown route 401 with no/wrong token, same route 404 (past auth, into Fastify's router) with the correct token, `x-correlation-id` present on every response, structured JSON logs with `reqId`/`service`/`env` fields visible throughout.
- **`prd.md` Task 5 checked off; `progress.md` updated in the same pass.**

### 2026-08-24
- **User asked to create a branch named "Yasin" and push all uncommitted work to it.** Flagged the direct conflict with `CLAUDE.md`'s standing rule ("Never commit or push. The user commits, nobody else.") and that `origin` is a real shared GitHub repo (`abdullahjay/WP_lementor_MCP`), and asked which way to resolve it. The question was declined/interrupted without an answer, and the user moved on to resuming task work instead — **this request is still open, nothing was committed or pushed, and none of the branch/push discussion should be inferred as resolved.** Raise it again before assuming either way next time it comes up.
- **EMCP-006 — MCP protocol conformance — DONE.** New `server/src/protocol/`: `types.ts`, `errors.ts`, `meta.ts`, `origin.ts`, `registry.ts`, `route.ts`, `route.test.ts`. Wired into `buildServer()` (`server/src/http/server.ts`) via `registerMcpRoute(app, new MethodRegistry())`, alongside (not replacing) EMCP-005's header auth and correlation ID hooks.
  - `POST /mcp`: validates `MCP-Protocol-Version`/`Mcp-Method`/`Mcp-Name` headers against the body's `_meta` block and the request's own `method` (400 + `-32020` on any mismatch, including an unsupported protocol version — folded into the same code since prd.md's AC only names one), validates `Origin` when present (403 if not on `MCP_ALLOWED_ORIGINS`, allowed when absent since non-browser clients like `mcp-remote`/Claude Code's local bridge don't send one and the check exists for DNS-rebinding, not general auth), dispatches through `MethodRegistry` (404 + `-32601` for an unregistered method), and stamps `resultType` onto every successful result centrally in `dispatch()` rather than per-handler. `GET`/`DELETE` on `/mcp` return `405` — Streamable HTTP under 2026-07-28 has no session/SSE stream on this transport (`solution.md` §3), so those verbs are refused outright rather than 404ing ambiguously.
  - `tools/list` ships built into the registry, currently returning an empty array — real tools are EMCP-007+. It still carries `cacheScope: "private"` and a `ttlMs`, per this task's literal AC ("`ttlMs` and `cacheScope` on `tools/list`"); `Blueprints.md` §7's denser language about per-tool `ttlMs` (long for read-mostly tools, `0` for page-derived ones) is a separate concern for when actual tools register themselves — noted here so it isn't lost, not built yet.
  - **Two real bugs found while verifying, both fixed:** (1) `npm run test` was silently running a stale, duplicated copy of `auth.test.ts` compiled to `dist/` by EMCP-005's earlier smoke test — vitest's default include glob doesn't exclude build output. Added `server/vitest.config.ts` (`exclude: ['dist/**', 'node_modules/**']`), which also raised `testTimeout` to 20s after observing this container's bind-mounted-workspace-under-Docker-Desktop-on-Windows I/O is slow enough to blow the 5s default under load (same assertions run in single-digit ms once loaded — not a logic bug, an environment one). (2) The first "green" `verify:unit` run wasn't actually green — piping the command through `| tail -N` for readability made the reported exit code `tail`'s, not `npm`'s, silently swallowing a real lint failure. Re-ran without the pipe (explicit `echo EXITCODE=$?`) to get a trustworthy result before trusting it.
  - **Verification:** `npm run verify:unit` genuinely green this time (18 tests, 2 files, exit code confirmed). Also live-smoke-tested the built server end to end: `ping` and `tools/list` succeed with matching headers/`_meta`; header/`_meta` mismatch → `400`/`-32020`; unknown method → `404`/`-32601`; `GET`/`DELETE /mcp` → `405`; disallowed `Origin` → `403`; no `Authorization` → `401` (confirming EMCP-005's auth still applies on this route).
- **`prd.md` Task 6 checked off; `progress.md` updated in the same pass.**
- **EMCP-007 — First tool: `get_site_info` — DONE.** New `server/src/wp/{config,client}.ts`, `server/src/tools/getSiteInfo.ts`; extended `MethodRegistry` (`registerTool()`, built-in `tools/call`) and `route.ts` (catches `JsonRpcMethodError` from dispatch, maps to the right HTTP status, generic errors → 500/`-32603`) to support it.
  - **Fixed the permalinks gap flagged in the last handoff, at the root rather than in the client.** Added a `wp rewrite structure '/%postname%/' --hard` + `wp rewrite flush --hard` step to `scripts/provision.sh` (idempotent, applied to both sandboxes) instead of teaching the Node HTTP client to fall back to `?rest_route=`. Confirmed live: `GET /wp-json/emcp/v1/site` now returns `401` (reached the route, rejected for auth) instead of `301` on both sandboxes.
  - **Site targeting is env-var-only for now**, matching the project's own phasing: `solution.md` §3's "one connector URL per site" / "no `site_id` argument" model isn't backed by a real site registry until EMCP-013+, so `WP_BASE_URL`/`WP_AUTH_USER`/`WP_AUTH_APP_PASSWORD` (added to `.env.example` with guidance, no real secret committed) stand in for it locally — this Node server process is bound to whichever one site those three vars point at, which is the same shape the eventual per-site connector URL will have, just not backed by OAuth/registry yet.
  - **Tool result shape follows the real MCP `CallToolResult` convention** (`content[]` + `structuredContent` + `isError`), not something invented — Blueprints.md §7's "results also carry serialized JSON in a text block for compatibility" is exactly this shape. A WordPress-side failure (unreachable, non-2xx) is caught inside the handler and reported as `isError: true` in a normal `200` result; it does **not** throw. Only "unknown tool" and "malformed `tools/call` params" go through the JSON-RPC error channel (`404`/`-32601` and `400`/`-32600` respectively) — this split is Blueprints.md §8.1's channel table applied to the one real tool that exists now, and it's asserted by both a unit test (mocking WP-unreachable via an unset `WP_BASE_URL`) and the live smoke test below.
  - `tools/list`'s per-tool metadata (`cacheScope: "private"`, `ttlMs: 300_000` — 5 minutes, since site capabilities change rarely) is genuinely on each tool descriptor now, not just the envelope EMCP-006 already had.
  - **Verification:** `npm run verify:unit` green (21 tests, exit code confirmed explicitly — see EMCP-006's note about not trusting a piped command's exit code, followed again here). Live end-to-end, both sandboxes: built the server, pointed it at `wp-v4-pro` via env vars with a freshly generated Application Password, called `tools/call` → `get_site_info` over the real `/mcp` route and got back `generation_default: "v4"`, `isError: false`; repointed at `wp-v3-free` with its own Application Password, got `generation_default: "v3"` — the full path (Claude-shaped JSON-RPC call → Node → WordPress plugin → back) proven end to end, not just each half in isolation. `tools/list` confirmed to surface the tool's full descriptor (`outputSchema`, `annotations`, etc.). `debug.log` stayed empty on both sandboxes throughout.
- **`prd.md` Task 7 checked off; `progress.md` updated in the same pass.**
- **EMCP-008 — Capture golden fixtures ⚠️ — STARTED (1/10).** User is building the fixture pages by hand in Elementor's real editor per fixture; each gets captured via `wp post meta get <id> _elementor_data` and written to `tests/fixtures/`, new this session along with `tests/fixtures/README.md` (format + per-fixture status table + the same "never edit a fixture, flag Blueprints.md instead" rule restated locally).
  - **`v4-atomic` captured** from `wp-v4-pro` post 5 — an `e-flexbox` containing a populated `e-heading` and `e-button` with real text content and local styling. Confirms `Blueprints.md` §3.2's nested-typed-props claim for text content **exactly**: `{"$$type":"html-v3","value":{"content":{"$$type":"string","value":"Buil faster"},"children":[]}}` (the "Buil faster" typo is verbatim what was typed in the editor — left as-is, a fixture should be exactly what Elementor produced, not cleaned up).
  - **⚠️→✅ NEEDS-REVIEW, now resolved:** empty/unset `settings`, `styles`, `interactions`, and `editor_settings` fields serialize as `[]` (an array), not `{}`, on both the container and any widget nobody has touched yet — PHP's classic empty-array-vs-associative-array-to-JSON behavior leaking through. `Blueprints.md` §3.2 and `solution.md` §5.3 didn't note this. User reviewed the proposed correction and approved it as written (2026-08-24) — both docs now updated with a matching bullet citing this fixture as the source.
  - Also observed, not necessarily a bug, just worth recording: styled elements gain a `classes` key in `settings` (`{"$$type":"classes","value":["e-<id>-<hash>"]}`) pointing at their own entry in the local `styles` object; unstyled elements have neither. And `settings` key **names differ per widget type** even for conceptually-similar fields — the heading's text lives under `title`, the button's under `text`. Both consistent with `describe_widget`-driven introspection already being the documented plan (`Blueprints.md` §2.3), not a contradiction — just confirmed with real data now instead of assumed.
  - **Two real capture-flow mistakes along the way, both benign, recorded because they cost real back-and-forth:** the user's first attempt (fixture #3, meant for `v4-atomic`) was actually built on `wp-v3-free` and turned out to be Elementor's own auto-import of existing post content into a single Container > legacy Text Editor widget when "Edit with Elementor" is clicked on a page that already has content — not V4 at all, and on the wrong (V3-forced) sandbox. Caught before writing anything by checking `elType`/`widgetType` against the `e-` prefix rule rather than trusting the fixture number alone. Second: two "done" reports needed a `post_modified` timestamp check to actually confirm a save had landed in the database — assuming a claimed edit persisted, without checking, is exactly the failure mode this whole task exists to design out of the rest of the system too.
  - **`legacy-section-column` captured** from `wp-v3-free` post 2 — `elType: "section"` / `"column"`, flat widget `settings` (`"title":"Build it on V3 / Free"`, `widgetType: "heading"` — no `$$type`, no `e-` prefix). Confirms `Blueprints.md` §5.1's legacy-shape claim.
    - **Real, previously-undocumented fact worth keeping:** a fresh install's "Add New Section" only offers Flexbox/Grid — there is no UI path to a legacy Section/Column on a site where the `container` experiment is active (which is every fresh install's default). Deactivating `elementor_experiment-container` (`wp option update elementor_experiment-container inactive`) is what unlocks the old layout picker. Immediately **re-activated it after capture** so `wp-v3-free` goes back to its intended V3-container-default state for the fixtures still pending there (`v3-container`, `mixed-v3-v4`, `mixed-legacy-v3`). Worth adding to `CLAUDE.md`'s gotcha list once EMCP-008 wraps up.
    - Two false starts before this capture, both on the user's side and both caught before anything got written: first attempt was on the wrong sandbox (`wp-v4-pro`, confirmed by recognizing the `v4-atomic` fixture's own "Buil faster" content in a screenshot) with the experiment toggle having no effect there since it was only changed on `wp-v3-free`.
  - **`v3-container` + `mixed-legacy-v3` captured together from one save.** User added a Container (heading + icon) to the *same* `wp-v3-free` page that still had the `legacy-section-column` section on it. Rather than ask for a rebuild: extracted just the new `elType: "container"` subtree as `v3-container`, and separately saved the whole two-element page (legacy section + container together) verbatim as `mixed-legacy-v3` — captured out of sequence, but from data that would otherwise have been overwritten by the time the sequence reached it. Confirms `Blueprints.md` §5.2's V3-container shape (flat settings, `elType: "container"`, no `$$type`).
  - **`mixed-v3-v4` captured** from `wp-v4-pro` post 5. First attempt replaced the page's existing atomic content entirely rather than adding alongside it (caught via `post_modified` + a full data check — the earlier `e-flexbox` heading/button were simply gone), so it was redone. Second attempt succeeded and produced a stronger case than planned: `e-heading`/`e-paragraph` (V4 atomic) ended up as **direct sibling children of a V3 `elType: "container"`**, not a separate top-level V4 section next to a separate V3 one. Elementor allowed this without complaint — atomic content widgets aren't restricted to atomic-layout parents. This is a better test of "detection is per-node, not per-document" (`Blueprints.md` §5.2) than what was asked for, since it forces correct behavior even one level down inside a single shared parent. Setup needed the same toggle-off/toggle-on trick as `legacy-section-column`, this time on `e_atomic_elements`/`e_opt_in_v4`/`e_opt_in_v4_page` — both fully restored to active afterward.
  - **`nested-widget` investigated, confirmed blocked — real discrepancy, not user error.** `nested-tabs`/`nested-accordion` widgets exist in the PHP registry (`\Elementor\Plugin::$instance->widgets_manager->get_widget_types()` lists both, both report `show_in_panel(): true`, and the `nested-elements` experiment is active) but genuinely do not appear in the live editor's widget panel — confirmed twice: once via panel search ("No widget found for 'Nested'"), once by manual scroll with no search term at all. Along the way, found Elementor's own internal JS error log (`elementor_log` option) recorded two real client-side errors in `editor.min.js` around the same time (`"this.ui.input.val is not a function"`, `"Cannot read properties of undefined (reading 'toLowerCase')"`) — plausibly related, but this couldn't be confirmed further without browser devtools access, which this session doesn't have. **Not substituting classic Tabs/Accordion** — those use a repeater field for tab content, not real nested Elementor elements as `elements` children, so they wouldn't exercise what `Blueprints.md` §5.5 actually needs this fixture to test. Left blocked in `tests/fixtures/README.md` rather than faked.
  - **`unicode-roundtrip` captured** from `wp-v4-pro` post 5 (a new `e-paragraph` alongside the `mixed-v3-v4` content already there, extracted in isolation). Content: `"Design" — reimagined. مرحبا بالعالم / 你好世界` — em-dash, Arabic (RTL), CJK all confirmed surviving as correctly UTF-8-escaped JSON in `_elementor_data` (`—`, `م...`, `你...`). Used straight quotes rather than curly ones, so curly-quote punctuation specifically isn't covered — not worth another round-trip for. **Important scope note:** this only confirms Elementor's own storage survives real Unicode content; the actual snapshot → rollback → re-read round-trip `Blueprints.md` §6.6 and `solution.md` §11 describe this fixture for doesn't exist as a mechanism yet (that's EMCP-037, safety-ring phase). Revisit this fixture then rather than treating it as fully exercised now.
  - **`responsive-widescreen` captured** from the same `wp-v4-pro` post 5, same `e-paragraph` element as `unicode-roundtrip` — now carrying local `styles` with four breakpoint variants (`widescreen`: 15px, `desktop`: 14px, `tablet`: 13px, `mobile`: 12px font-size). Widescreen had to be added to Site Settings → Layout → Breakpoints first — off by default on both sandboxes, confirmed matching what `get_site_info` already reported live back in EMCP-004 (`enabled: false`). Important scope note recorded in the fixture's own provenance: the raw JSON only names the breakpoint (`"widescreen"`); the min-width-vs-max-width **direction** that makes this fixture meaningful comes from Elementor's `Breakpoints_Manager`, not from this data — already separately confirmed live in EMCP-004 (`widescreen` → `direction: "min"`, everything else → `"max"`). A compiler test using this fixture needs both pieces together.
  - **`deep-nested` captured** from `wp-v4-pro` post 5 — 5 real `elType: "container"` levels (`c8a1583` → `a5bdf07` → `3d93511` → `30201b8` → `1638085`), each `isInner: true` except the outermost, ending in an `e-heading` widget saying "Level 5". First attempt was caught and redone: the user initially added a heading with that exact label but as a flat top-level widget, no real nesting at all — found by inspecting the captured structure rather than trusting the label, same pattern as the `mixed-v3-v4` mistake earlier in this session. Second attempt, guided step-by-step (drag a Container into a Container, five times), produced genuine depth, visually confirmed in a screenshot showing five nested dashed boxes before the data was even pulled.
  - **EMCP-008 explicitly kept open by the user.** Asked directly whether "8/10 + 2 documented blockers" was good enough to close for now — user said keep it open and move on. So: **not marked `[x]` in `prd.md`, and not to be closed later without the user saying so explicitly.** Correctly treated as a still-open dependency: did not proceed to EMCP-009 (which depends on it), moved to the next task whose dependencies were already satisfied instead (EMCP-010).
  - **EMCP-010 — Freeze plugin REST contract v1 — DONE.** New `server/src/wp/contract.ts` (`MINIMUM_PLUGIN_VERSION`, `assertPluginVersionCompatible()`, `PluginVersionMismatchError`), wired into `client.ts`'s `getSite()` — checked against the plugin's own `plugin_version` on every call that reaches it, not just once, since there's no persistent connection to gate under MCP 2026-07-28 (no session/`initialize`, `solution.md` §3) — this **is** the closest equivalent of "connect time" this transport has. `getSiteInfo.ts`'s error handling special-cases `PluginVersionMismatchError` for an actionable message naming both versions, distinct from a generic WordPress-unreachable error.
    - `Blueprints.md` §6 already stated this exact behavior as intended ("the server declares a minimum plugin version and fails loudly on mismatch at connect time") — this task implements what was already specified, plus a concrete implementation note (file, constant name, mechanism) added in the same edit per the standing rule on REST-contract changes.
    - **Verification:** `npm run verify:unit` green (33 tests — 9 new in `contract.test.ts` covering the pure version-comparison logic including a numeric-not-lexical edge case (`"0.10.0"` > `"0.9.0"`), 3 new in `client.test.ts` mocking `fetch` to exercise the actual wiring: compatible version passes through, older version throws `PluginVersionMismatchError`, non-2xx still throws `WordPressApiError`). Live smoke-tested against `wp-v4-pro`: `get_site_info` still succeeds end-to-end (`isError: false`) with the version gate in place, since the plugin's `0.1.0` matches the server's `MINIMUM_PLUGIN_VERSION` exactly — didn't get to observe a live mismatch (would need an artificially bumped constant or a downgraded plugin, neither worth doing just to watch it fail when the unit tests already prove the logic).
- **`prd.md` Task 10 checked off; `progress.md` updated in the same pass.**
- **EMCP-008 closed by explicit user decision.** One more diagnostic pass on `nested-widget` first: checked both widgets' category (`general`, `active: yes` — same active-category status as `heading`, which does render fine) and the full registered-category list (nothing named "Pro"-gated or inactive that would explain it). No difference found between why `heading` renders and `nested-tabs`/`nested-accordion` don't — reinforces that this is the client-side JS bug already suspected (`elementor_log`'s `editor.min.js` errors), not a registration/category/capability gate, and not something diagnosable further without real browser devtools access (offered to the user; not pursued further this session). Asked directly whether to keep chasing it or close the task with both remaining fixtures as accepted, permanent blockers — **user chose to close.** `prd.md` Task 8 now checked `[x]`. The blocker documentation in `tests/fixtures/README.md` and this file's earlier Log entries stays as-is — closing the task recorded a decision, it didn't erase the findings.
- **`prd.md` Task 8 checked off; `progress.md` updated in the same pass.**
- **EMCP-009 — Fixture immutability guard — DONE.** New top-level `package.json`/`vitest.config.ts` (repo root, not inside `server/` — first genuinely cross-cutting harness, deliberately kept separate from the Node server's own project since it isn't Node-application code). `tests/fixtures/hashes.json` — a SHA-256 manifest over the full raw bytes of each of the 8 fixture files, `algorithm`/`note`/`hashes` shape. `tests/unit/fixtures.test.ts` — three tests: every fixture file has a manifest entry and vice versa (catches additions/deletions that forgot to update the manifest), every fixture's actual hash matches its recorded one, and a dedicated test proving the mechanism itself fires on a real change — mutates an isolated temp copy (never the committed fixture, which would violate the very rule being tested) and asserts its hash differs from the recorded one.
  - `_provenance.hash` inside each fixture stays `null` permanently, by design — the manifest hashes the whole file, so embedding the real hash inside itself would be self-referential. Clarified in `tests/fixtures/README.md`, which previously implied (from EMCP-008) that field would get filled in.
  - **Verification, done properly this time:** ran `npm run test` clean (3/3, exit code confirmed without a pipe). Then actually proved the guard fires for real — not just via the isolated-temp-copy test — by deliberately corrupting `deep-nested.json`'s recorded hash in the real manifest, confirming the suite failed (2/3 red, both failures pointing at the tampered entry), then restoring the correct hash and reconfirming green. Caught the `tail`-pipe-masks-exit-code pitfall recurring during this — same mistake as EMCP-006, now doubly on the record.
- **`prd.md` Task 9 checked off; `progress.md` updated in the same pass.**
- **EMCP-011 — Verification harness split — DONE.** Root `package.json` now orchestrates all three independent projects: `test`/`verify:unit` runs the repo-root fixture-guard suite, then `npm --prefix server run verify:unit`, then `composer --working-dir=plugin test` — stops at the first failure (`&&`-chained), so a broken lower layer is unambiguous rather than buried under later output.
  - **`plugin/`'s PHPUnit had a dev dependency but nothing to actually run** — no `phpunit.xml`, no `tests/` directory at all, so `composer test` would have failed outright. Fixed properly, not just plumbed: `plugin/phpunit.xml`, `plugin/tests/bootstrap.php`, `plugin/tests/stubs.php` (hand-rolled `WP_Error`/`WP_REST_Request`/`current_user_can()`/`__()` — just enough surface to unit-test real logic, not a WordPress bootstrap, keeping this genuinely in the "unit: no network" bucket per `ralphloop.md`'s Testing Guidance), and `plugin/tests/CapabilitiesTest.php` — 5 real tests against `Capabilities::can_read_site()`'s actual security-relevant branches (missing header, empty header, present-header-without-capability, present-header-with-capability), not placeholder assertions.
  - **`scripts/verify-live.sh`** checks each sandbox's container health via `docker compose ps`, then calls the real plugin REST route (`GET /wp-json/emcp/v1/site`) with Basic auth, asserting both a 200 and a shape check (`generation_default` present) — catches contract drift, not just reachability. Emits a `VERIFY_LIVE=PASS`/`FAIL (N check(s) failed)` line for machine-readability, per the AC. Generated one **persistent** named Application Password per sandbox (`emcp-dev-verify-live`, replacing the pile of ephemeral ones from manual testing all session — revoked those first) and added `WP_V4_PRO_AUTH_APP_PASSWORD`/`WP_V3_FREE_AUTH_APP_PASSWORD` to `.env`/`.env.example`.
  - **Verification, thoroughly:** ran the full orchestrated `verify:unit` clean — 3 (root) + 33 (server) + 5 (plugin) = 41 tests, exit code confirmed without a pipe. For `verify:live`, didn't just run it once against healthy sandboxes — proved all three states for real: (1) missing credential (blanked `.env` temporarily, restored after) → clear actionable failure naming the exact env var and the command to fix it; (2) sandbox actually stopped (`docker compose stop wp-v3-free`, restarted after) → "container not running at all, run docker compose up -d"; (3) container up but still starting (caught mid-restart) → a *third*, distinct message ("reports 'starting', not healthy") that wasn't even deliberately tested for, just observed — a genuine bonus confirmation the health-state handling is real, not just the two paths that were planned.
- **`prd.md` Task 11 checked off; `progress.md` updated in the same pass.**
- **EMCP-012 — Local development documentation — DONE. User reviewed `README.md` (read-through) and confirmed.** Before writing a word of it, found and fixed a real gap: `docker-compose.yml`'s `mcp` service was still EMCP-001's placeholder inline Node HTTP server, never actually replaced when `server/` became real (EMCP-005 explicitly deferred it, and no later task picked it back up). A README claiming "run `docker compose up -d`, then call `get_site_info`" would have been **false** as written — closing that gap was necessary for this task's own acceptance criterion to be true, not scope creep.
  - **New `docker/mcp/Dockerfile`** — two-stage build (`npm ci` + `npm run build` in a build stage, `npm ci --omit=dev` + copied `dist/` in the runtime stage). `docker-compose.yml`'s `mcp` service now builds from it, with `WP_BASE_URL`/`WP_AUTH_USER`/`WP_AUTH_APP_PASSWORD`/`MCP_HEADER_AUTH_TOKEN`/`LOG_LEVEL` sourced from `.env`, and a real healthcheck (`node -e` hitting `/healthz` — no `curl`/`wget` in the `bookworm-slim` base, so used Node's own `http` module instead of adding a package for it).
  - **Real build failure hit and fixed, now in `CLAUDE.md`:** first build attempt failed on `invalid file request server/node_modules/.bin/acorn` — the build context (repo root) had no `.dockerignore`, so `server/node_modules` (125MB+, containing a symlink Docker's context transfer choked on) got sent into every build. Added a repo-root `.dockerignore` excluding `node_modules`/`dist`/`vendor`/`.git`; second build succeeded clean.
  - **Verified for real, not asserted:** built the image, brought the container up, confirmed `docker compose ps` reports `healthy`, then called `get_site_info` through `tools/call` against `http://localhost:3000/mcp` **from the host** — the same request shape a real MCP client would send, not a `dev`-container-internal smoke test like every prior live check this session. Got back `isError: false` with the full site payload. This is the first time this session that "the stack, freshly brought up, actually does the thing" was proven end-to-end rather than proven per-piece.
  - **README.md rewritten wholesale** (was still `EMCP_001`-era placeholder text): prerequisites, first-time setup, Application Password generation for the `mcp` container specifically (distinct from the per-sandbox ones `scripts/verify-live.sh` uses), both verification harnesses with a note that `verify-live.sh` must run from the host (no Docker socket in `dev`), the exact `get_site_info` curl call as the concrete proof-of-life, a client-connection section that's honest about not knowing the exact Claude Code CLI flag syntax for this MCP revision rather than inventing a plausible-looking command, reset, a project-layout table, and a troubleshooting pointer to `CLAUDE.md` instead of duplicating it.
  - **User reviewed via read-through (not a full clean-checkout teardown-and-retry)** — offered both options explicitly, including being upfront that the rigorous option would wipe all provisioned sandbox state via `docker compose down -v`; user chose the read-through. Confirmed. `prd.md` Task 12 now checked `[x]`.
- **`prd.md` Task 12 checked off; `progress.md` updated in the same pass.**
- **EMCP-013 — Database schema and migrations — DONE.** New `server/src/db/{schema,config,index,migrate}.ts`, `server/drizzle.config.ts`, `server/drizzle/0000_acoustic_vampiro.sql` (+ hand-written `.down.sql` counterpart, `meta/`), `server/src/db/schema.test.ts`.
  - All 7 tables from Blueprints.md §10 / solution.md §11.3: `sites`, `grants`, `credentials`, `ledger_index`, `idempotency_keys`, `approval_tokens`, `preview_nonces`. Every site-scoped table (all but `sites` itself) has a `site_id` FK (`ON DELETE cascade`) **and** its own index — solution.md §17's "site_id must be part of every cache key, storage path and index" applied concretely, not just cited. `grants` and `idempotency_keys` additionally get a unique composite index on `(subject, site_id[, key])`, matching the `(subject, site)`-scoped lookup both are specifically documented to need. Approval tokens and preview nonces store a **hash** of their token, never the raw value — consistent with "credentials never logged" extended to our own database, not just WordPress's.
  - **Reversibility, since drizzle-kit only generates forward SQL:** each migration gets a hand-written `<name>.down.sql` sibling, and `server/src/db/migrate.ts` is a small custom runner (`up`/`down` argv) — `up` uses `drizzle-orm`'s real migrator, `down` looks up the most recently applied migration in `drizzle.__drizzle_migrations` and runs its `.down.sql` counterpart directly via the pool, then removes that tracking row. Noted a real limitation in the code comment: the "which file is `down`" mapping currently assumes migrations are applied in file order (true today, with exactly one), and will need revisiting once a second migration exists.
  - **`db:config.ts`'s host/port needed a real fix, not a placeholder:** `POSTGRES_HOST`/`POSTGRES_PORT` in `.env` are for connecting from the **host** (`localhost:5433`, outside Docker); code running inside `mcp`/`dev` needs the docker-compose service name and Postgres's container-internal port (`db:5432`) instead — added `POSTGRES_HOST` (defaults `db`) and `POSTGRES_INTERNAL_PORT` (defaults `5432`) so the two never get confused, and documented the distinction inline since it's exactly the kind of thing that looks like it should just work and doesn't.
  - **Real gap found and fixed: `.env` values are not automatically container environment variables.** Docker Compose only uses `.env` for `${VAR}` substitution *inside* `docker-compose.yml` itself — `mcp` and `dev` both needed explicit `POSTGRES_*` entries added to their own `environment:` blocks before `process.env` inside either container actually had them. Recreating `dev` to pick this up also triggered `db` to recreate (config-hash change via `.env`) — confirmed its named volume (`postgres-data`) preserved data across that, no loss.
  - **Verification, thoroughly — not just "it generated," proven live:** ran the real up migration against the actual `db` container, confirmed all 7 tables and every expected `site_id` index exist via `psql \dt`/`\di`. Then ran `down`, confirmed `psql \dt` reports zero tables. Then ran `up` again, confirmed all 7 tables back — a genuine round trip, not just "the down script looks like it should work." Also added `schema.test.ts` (8 tests, pure Drizzle introspection via `getTableConfig`, no DB connection — keeps regression coverage in the automated suite itself, not just this session's one-off manual `psql` check) and ran the full orchestrated root `verify:unit` (49 tests: 3 root + 41 server + 5 plugin) clean afterward.
- **`prd.md` Task 13 checked off; `progress.md` updated in the same pass.**
- **EMCP-014 — Site registry — DONE.** New `server/src/registry/{slug,reader,admin}.ts` + `slug.test.ts` + `admin-boundary.test.ts`.
  - **The read/write boundary ("no tool can mutate the registry") is enforced structurally, not just documented:** `reader.ts` exports only `getSiteBySlug`/`listSites`; every mutation (`createSite`) lives in `admin.ts`, a separate file whose own docblock says never to import it from `server/src/tools/`. `admin-boundary.test.ts` scans every non-test file under `src/tools/` for that import and fails if found — currently trivially true (only `get_site_info` exists and doesn't touch the registry at all yet), but the test exists so a *future* tool violating this fails a test, not a code review someone might skip.
  - `generateSiteSlug()` — 128 bits of `crypto.randomBytes`, base64url. Tested three ways: format, 1000-call uniqueness, and no-shared-prefix-across-consecutive-calls (guards against a sequential/timestamp-derived implementation slipping in later, not just against this one being wrong today).
  - **Deliberately did not reconcile this with `get_site_info`'s existing env-var-only site targeting (EMCP-007).** The registry now exists but nothing reads from it yet — `WP_BASE_URL`/`WP_AUTH_USER`/`WP_AUTH_APP_PASSWORD` are still how the server knows which site to call. Wiring the two together is a real design decision (does a grant's site come from the registry now, replacing the env vars entirely, or do both coexist for a while?) that belongs with EMCP-016 (Grant resolution) or later, not silently decided as a side effect of this task.
  - **Verification:** `verify:unit` clean (45 tests: 4 new). Live-verified beyond the unit tests — wrote a throwaway script (`server/scratch-registry-check.ts`, deleted immediately after, never committed) that ran `createSite` → `getSiteBySlug` → `listSites` against the real `db` container, confirmed the fetched row matched the created one, confirmed the slug looked genuinely random (e.g. `HuFzFfjBtND5Pd0xuJitDA`), then deleted the test row and confirmed via `psql` that `sites` was empty again — left no residue for the next task to trip over.
- **`prd.md` Task 14 checked off; `progress.md` updated in the same pass.**

---

## Session Handoff

**Next task:** EMCP-015 — Credential encryption (depends on EMCP-013, done).

`sites` and the read/write registry boundary both exist now. EMCP-015 is `server/src/credentials/`: envelope encryption, per-site DEK, KEK sourced from **outside** the database (a KMS or equivalent — solution.md §9.4, and the `credentials` table's `encrypted_dek` column already exists to receive the result). Two AC worth planning around before writing code: **credentials must never reach any log sink** (worth a test that actually asserts this, e.g. spying on the logger and confirming a credential value never appears in any log call — not just "we didn't log it on purpose") and this is explicitly a **local-KMS-simulation problem for dev** (there's no real KMS in this Docker Compose stack) — decide up front whether that's an env-var-sourced master key (simplest, dev-only, clearly labeled as such) or something more elaborate, and don't over-build a fake KMS abstraction beyond what dev actually needs.

**What changed underneath EMCP-013, worth knowing before touching `docker-compose.yml`, `.env`, or `server/src/db/` again:**
- `mcp` and `dev` both now have explicit `POSTGRES_*` entries in their `environment:` blocks — `.env` alone does **not** make a var available inside a container's `process.env`, only `${VAR}` substitution in the compose file itself. This bit EMCP-013 once already; don't assume a new `.env` var is "just there" for application code without adding it to the relevant service's `environment:` block too.
- `POSTGRES_HOST`/`POSTGRES_INTERNAL_PORT` (container-internal: `db`/`5432`) are distinct from `POSTGRES_PORT` (host-mapped: `5433`) — `server/src/db/config.ts` uses the former; scripts run from the host (like `verify-live.sh`) would need the latter if they ever needed direct Postgres access, though none currently do.
- Recreating `dev` recreated `db` too (config-hash change), and that's expected/safe — named volumes persist. Don't be alarmed by it, but do verify data survived if it happens again, same as this time.
- Running a throwaway `tsx` script against `server/`'s code needs to live **inside** `server/` (relative imports resolve from the script's own location) and have a `.ts`/`.mts` extension recognized as ESM — a script in `/tmp` fails on both counts. Delete it immediately after use; don't let scratch scripts accumulate in the repo.

**Outstanding, not blocking:**
- **The "Yasin" branch/push request from 2026-08-24 is still unresolved** — nothing committed or pushed since. Don't assume an answer either way; ask again if it's still relevant.
- Elementor Pro zip still not supplied — blocks `pro-globals-dynamic` (accepted as a permanent blocker, EMCP-008) and `SiteController::pro_tier()` beyond `'free'`/`'pro-tier-unresolved'`.
- `typescript` is pinned to `^6.0.3` instead of the newest `7.0.2` (EMCP-005). Revisit when `typescript-eslint` supports TS 7.
- Always check a verify command's **actual exit code**, not just its tail output — piping through `tail` has hidden a real failure more than once now (EMCP-006, and again mid-EMCP-009 before being caught). Use `; echo EXITCODE=$?` or otherwise avoid a pipe defeating the check.
- Killing a background test-server process in the `dev` container by `pkill -f`/pattern-matching `/proc/*/cmdline` is unreliable here (busybox `pkill`, and a naive pattern can match the killer's own command line). Find the exact PID first, then `kill -9` it, excluding `$$`.
- Three separate `node_modules` now exist (repo root, `server/`) plus `plugin/vendor` — normal for this multi-project layout, not a mistake, but worth knowing before assuming a single `npm install` at any one level covers everything.

**Read first:** `ralphloop.md`, then `CLAUDE.md`, then the `Blueprints.md` sections your task touches.

**Most important thing to know:** every claim in the design documents about Elementor's data shapes is reasoned from documentation and **has not been verified against a real install**. EMCP-008 is where that changes. When captured fixtures disagree with `Blueprints.md` §3.2 or §5, the blueprint is probably wrong — record it and mark NEEDS-REVIEW. Do not change code to match a fixture until a human has ruled on which is correct.
